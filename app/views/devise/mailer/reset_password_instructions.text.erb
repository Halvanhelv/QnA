Choose a new password

Someone asked to reset the password for <%= @resource.email %>. Open this link to choose a new one:

<%= edit_password_url(@resource, reset_password_token: @token) %>

If this was not you, ignore this email. Your password stays the same until you open the link and set a new one.
