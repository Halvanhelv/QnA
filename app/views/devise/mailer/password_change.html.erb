<%= mail_heading 'Your password was changed' %>
<%= mail_paragraph "The password for #{@resource.email} was just changed." %>
<%= mail_paragraph "If you did not do this, reset your password right away.", muted: true %>
<%= mail_button 'Reset password', new_password_url(@resource) %>
