- if @rewards.any?
  - @rewards.each do |reward|
    = reward.question.title
    p
    = reward.name
    p
    = image_tag rails_blob_path(reward.img, only_path: true), size: 200
    p --------------------------------------------------------------------
- else
  p
  = t('no rewards')
